function backgroundColorChecker {
    var mediaCardElements = new Array(document.getElementsByName(section))
    mediaCardElements.add
}
window.onload = backgroundColorChecker;
function isMediaCard (element) {
    return(element.getName ())
}